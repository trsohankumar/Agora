# Requires
The server requires the following libraries to be installed via brew
```
brew install spdlog
brew install nlohmann-json
```

To run this application
```
mkdir build
cd build
cmake ..
make
```